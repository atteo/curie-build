package com.example;

/** Greeter — the world's smallest library worth publishing. */
public class Greeter {
    /**
     * Build a friendly greeting for the given name.
     *
     * @param name who to greet
     * @return a greeting line
     */
    public String greet(String name) {
        return "Hello, " + name + "!";
    }
}
