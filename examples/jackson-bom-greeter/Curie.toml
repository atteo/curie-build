# jackson-bom-greeter-flat
#
# Demonstrates BOM imports inherited from the workspace
# (examples/Curie.toml).  This member declares only what's unique to it:
#
#   src/com.example/JacksonBomGreeter.java          — production source
#   tests/com.example/JacksonBomGreeterTest.java    — integration tests
#
# Versions for [dependencies] and [test-dependencies] entries whose value
# is "" are resolved from the workspace-inherited BOMs (jackson-bom for
# Jackson coordinates, junit-bom for org.junit.*).

[application]
name = "jackson-bom-greeter-flat"
version = "0.1.0"
mainClass = "com.example.JacksonBomGreeter"

[docker]
baseImage = "eclipse-temurin:21-jre-alpine"

[dependencies]
"com.fasterxml.jackson.core:jackson-databind" = ""

[test-dependencies]
"org.junit.jupiter:junit-jupiter"        = ""
"org.junit.jupiter:junit-jupiter-engine" = ""
